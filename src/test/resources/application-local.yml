spring:
  config:
    name: LOCAL

mandates-resolutions-ui:
  host: http://localhost:${server.port}

app:
  domain: ${mandates-resolutions-ui.host}

#Configuration flag (integration.mode) to use the mock or live integration
integration:
  mode: MOCK