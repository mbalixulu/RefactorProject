<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet version="1.0"
                xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
                xmlns:ns1="http://ws.online.fnb.co.za/v1/common/"
                xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <xsl:output method="xml" indent="yes"/>
    <xsl:strip-space elements="*"/>

   <!-- Original content exists -->
   <!-- This segment should reflect reordered buttons within the admin block -->
       <!-- Within the footer block, inside the `Otherwise Block` due to rules ``` 

           <symbol xsi:type="ns1:footer" ns1:text="" ns1:textAlign="left" ns1:buttonAlign="right"> <xsl:choose> .... into specific order- Inquiry resolution follows up``-->--> within updated version